/**
 * Mass Join Protection Module
 * 
 * Tracks join velocity and trips a lockdown if too many users
 * join within a short configurable time window.
 */
import _reexport from "./helpers/massJoin";
export default _reexport;
